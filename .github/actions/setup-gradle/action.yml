name: Set up Gradle
description: Prepare Java 8 Gradle builds

runs:
  using: composite
  steps:
    - uses: gradle/actions/wrapper-validation@v6
    - uses: actions/setup-java@v5.0.0
      with:
        distribution: temurin
        java-version: 8
        cache: "gradle"
